development:
  host_url: <%= ENV['HOST_URL'] || 'http://localhost:3000' %>
  stripe_publishable_key: <%= ENV['STRIPE_PUBLISHABLE_KEY'] || Rails.application.credentials.stripe&.dig(:publishable_key) %>
  stripe_secret_key: <%= ENV['STRIPE_SECRET_KEY'] || Rails.application.credentials.stripe&.dig(:secret_key) %>

production:
  host_url: <%= ENV['HOST_URL'] || 'http://20.244.43.88' %>
  stripe_publishable_key: <%= ENV['STRIPE_PUBLISHABLE_KEY'] || Rails.application.credentials.stripe&.dig(:publishable_key) %>
  stripe_secret_key: <%= ENV['STRIPE_SECRET_KEY'] || Rails.application.credentials.stripe&.dig(:secret_key) %>
